var myArray = [498,499,500,501,502];

var total = 0;

for ( var i = 0 ; i < myArray.length ; i++ ) {
    // add the current element to the total	
    total += myArray[i];
}

alert("The total is: " + total);

